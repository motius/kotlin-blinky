stages:
    - build

build:
    stage: build
    image: registry.motius.de/microservices/kotlin-native
    script:
        - sh build.sh
    artifacts:
        paths:
            - "build"
    tags:
        - staging-docker
